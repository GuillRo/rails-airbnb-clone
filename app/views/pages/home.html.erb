<div class="wrapper">
  <div class="home-container">
    <div class="banner"">
      <div class="banner-content">
        <h1 class="title-main">Find your next adventure</h1>
        <p class="catch-phrase">A new destination awaits.</p>
        <%# <a class="btn btn-primary btn-lg">Start now</a> %>

        <%= button_to "View all flats", flats_path, :id => "all_flats_button",
      :class => "btn btn-primary", :method => :get %>

      </div>
    </div>
  </div>

  <div class="home-container" id="suggestion-row">
    <div class="cards-row">
      <div>
        <%= render 'shared/card' %>
      </div>
      <div>
        <%= render 'shared/card' %>
      </div>
      <div>
        <%= render 'shared/card' %>
      </div>
    </div>
  </div>
</div>
